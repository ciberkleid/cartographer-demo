apiVersion: carto.run/v1alpha1
kind: ClusterSupplyChain
metadata:
  name: supplychain-web-image-knative
spec:
  selector:
    app.tanzu.vmware.com/workload-type: web-image-knative

  resources:

    - name: source-provider
      templateRef:
        kind: ClusterSourceTemplate
        name: image-repository

    - name: image-builder
      templateRef:
        kind: ClusterImageTemplate
        name: image-policy
      sources:
        - resource: source-provider
          name: source
#      params:
#        - name: semver
#          value: '>=1.0.0'

    - name: deployer-app
      templateRef:
        kind: ClusterTemplate
        name: app-deploy-kapp2knative
      images:
        - resource: image-builder
          name: image
#      params:
#        - name: containerPort
#          value: 80
